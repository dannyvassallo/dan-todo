import {
  pageTitle
} from '../../../helpers/page-title';
import { module, test } from 'qunit';

module('PageTitleHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = pageTitle(42);
  assert.ok(result);
});
