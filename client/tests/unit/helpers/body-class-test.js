import {
  bodyClass
} from '../../../helpers/body-class';
import { module, test } from 'qunit';

module('BodyClassHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = bodyClass(42);
  assert.ok(result);
});
